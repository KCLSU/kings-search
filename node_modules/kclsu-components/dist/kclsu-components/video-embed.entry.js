import { r as registerInstance, h } from './index-65ac344c.js';

const videoEmbedCss = ".dc-video-container{position:relative;padding-bottom:56.25%;padding-top:25px;height:0;overflow:hidden;margin-bottom:1em}.dc-video-container iframe,.dc-video-container object,.dc-video-container embed{position:absolute;top:0;left:0;width:100%;height:100%}";

const VideoEmbed = class {
    constructor(hostRef) {
        registerInstance(this, hostRef);
    }
    componentDidLoad() {
        if (!this.embedid && this.url) {
            const urlParams = new URLSearchParams(this.url);
            let vid = urlParams.get('v');
            this.videoId = vid;
        }
    }
    render() {
        let video;
        if (this.url)
            video = !this.videoId ? h("loading-spinner", { show: true }) : h("iframe", { frameborder: " 0", src: `https://www.youtube.com/embed/${this.videoId}` });
        return (h("div", { class: "dc-video-container" }, this.embedid ? h("iframe", { frameborder: " 0", src: `https://www.youtube.com/embed/${this.embedid}` }) : video));
    }
};
VideoEmbed.style = videoEmbedCss;

export { VideoEmbed as video_embed };
