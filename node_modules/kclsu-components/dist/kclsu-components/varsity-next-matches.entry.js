import { r as registerInstance, h } from './index-65ac344c.js';
import { a as removeParams } from './utils-cccb6bd3.js';

const VarsityNextMatches = class {
    constructor(hostRef) {
        registerInstance(this, hostRef);
    }
    renderMatchList() {
        return this.data.map(evt => {
            let style = {
                'margin': '1em'
            };
            return ([
                h("div", { style: style, class: "score-container" }, h("label-card", { cardtitle: evt.Title, reverse: true, image: removeParams(evt.ImageUrl), buttonlink: evt.Url, buttontitle: "Find out more" }))
            ]);
        });
    }
    render() {
        return (this.renderMatchList());
    }
};

export { VarsityNextMatches as varsity_next_matches };
