import { r as registerInstance, h } from './index-65ac344c.js';

const imageFitContainerCss = "img{width:100%;height:100%;object-fit:cover}";

const ImageFitContainer = class {
    constructor(hostRef) {
        registerInstance(this, hostRef);
    }
    render() {
        return (h("img", { src: this.src, alt: this.alt, class: this.classes ? this.classes : '' }));
    }
};
ImageFitContainer.style = imageFitContainerCss;

export { ImageFitContainer as image_fit_container };
