import { r as registerInstance, h, g as getElement } from './index-65ac344c.js';

const FullwidthStrip = class {
    constructor(hostRef) {
        registerInstance(this, hostRef);
        this.height = '';
        this.color = '#502765';
        this.styleElement = (styleProp, val) => {
            this.strip.style[styleProp] = val;
        };
    }
    componentDidLoad() {
        this.strip = this.fullStrip.querySelector('.relContent');
        if (this.height)
            this.styleElement('height', this.height);
        if (this.color)
            this.styleElement('background', this.color);
    }
    render() {
        return (h("div", { class: "row relContent" }, h("div", { class: "container" }, h("div", { class: "col-xs-12 col-md-8 col-md-push-2" }, h("slot", null)))));
    }
    get fullStrip() { return getElement(this); }
};

export { FullwidthStrip as fullwidth_strip };
