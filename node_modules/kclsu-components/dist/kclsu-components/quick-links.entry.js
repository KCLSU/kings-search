import { r as registerInstance, h } from './index-65ac344c.js';

const QuickLinks = class {
    constructor(hostRef) {
        registerInstance(this, hostRef);
        this.name = 'quick access.';
    }
    render() {
        return (h("fullwidth-strip", null, h("p", null, "Useful links for ", this.name, ":"), h("ul", null, h("slot", null))));
    }
};

export { QuickLinks as quick_links };
