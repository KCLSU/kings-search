import { r as registerInstance, h, g as getElement } from './index-65ac344c.js';

const SupportProgress = class {
    constructor(hostRef) {
        registerInstance(this, hostRef);
        this.maxtotal = 75;
    }
    componentDidLoad() {
        this.progressBar = this.el.querySelector('.ui-progressbar-value');
        let amt = Number(this.current) / Number(this.maxtotal) * 100;
        this.progressBar.style.width = `${amt}%`;
    }
    render() {
        return (h("div", { class: "col-md-6" }, h("div", { class: "well setwell" }, h("h5", null, " ", this.supportname, " "), h("div", { "aria-valuemax": this.maxtotal, "aria-valuemin": "0", "aria-valuenow": this.current, class: "ui-progressbar ui-corner-all ui-widget ui-widget-content" }, h("div", { class: "setcaption caption" }, " ", this.current + ' of ' + this.maxtotal, " "), h("div", { class: "ui-progressbar-value ui-corner-left ui-widget-header" })), h("p", null, "Petition Closed"))));
    }
    get el() { return getElement(this); }
};

export { SupportProgress as support_progress };
